// :ks_include_id: fb5da34edef1455388ca60e23308095f
. Configure the application running in the container to expose monitoring metrics that comply with the Prometheus specification at a specific service endpoint. For more information, refer to the link:https://prometheus.io/docs/instrumenting/clientlibs/[Prometheus official documentation].

. In the KubeSphere web console, set up a monitoring exporter on the service used to expose the workload to declare the path that provides the application's monitoring metrics. KubeSphere will retrieve the application's monitoring metrics based on the relevant parameters of the monitoring exporter.