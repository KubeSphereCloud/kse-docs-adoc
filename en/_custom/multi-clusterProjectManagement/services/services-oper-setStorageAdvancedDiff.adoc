// :ks_include_id: 417489540caa4044871d8fba1c13e801
. On the **Storage Settings** tab, mount volumes for the containers managed by the service backend workload, then click **Next**.

. On the **Advanced Settings** tab, specify nodes for the pods managed by the service backend workload, configure the service's external access mode, session affinity settings, and metadata, then click **Next**.

. On the **Cluster Difference Settings** tab, configure differential settings for the service across different clusters based on containers, ports, and environment variables, then click **Create**. After creation, the service will be displayed in the service list.