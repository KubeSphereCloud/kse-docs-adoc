---
title: "Pod IP Pool"
keywords: "Kubernetes, {ks_product}, Cluster Management, Pod IP Pool"
description: "Introduces how to manage cluster pod IP pools."
weight: 02
layout: "second"
---


This section introduces how to manage cluster pod IP pools.

A pod IP pool is used to assign IP addresses to pods. Each pod IP pool contains a private IP address range accessible within the cluster. You can specify a pod IP pool when creating workloads or Jobs.