// :ks_include_id: bc97a1c3d087413bba734c07a3edb221
. Log in to all cluster nodes and run the following command to edit the **/etc/hosts** file:
+
--
// Bash
include::../../../_ks_components/code/bash.adoc[]

vi /etc/hosts

include::../../../_ks_components/code/codeEnd.adoc[]

Add the following information in the **/etc/hosts** file to configure the domain name resolution rules for the private image service on the cluster nodes (replace `<registry IP address>` with the actual IP address of your private image service), and then save the file:

// Bash
include::../../../_ks_components/code/bash.adoc[]

<registry IP address> dockerhub.kubekey.local

include::../../../_ks_components/code/codeEnd.adoc[]
--