// :ks_include_id: 767f7ca66b4048cd98a9ff15a50b4d62
* You need to have resource viewing permissions for the platform, cluster, workspace, or project to view logs within a specific scope.

ifeval::["{file_output_type}" == "html"]

* The KubeSphere platform needs to have the Logging, Events, and Auditing extensions enabled.

* The KubeSphere cluster needs to have an Elasticsearch log receiver added.

endif::[]

ifeval::["{file_output_type}" == "pdf"]

* The KubeSphere platform needs to have the Logging, Events, and Auditing extensions enabled. For more information, refer to the "Extension Management" chapter in the *KubeSphere Platform Management Guide*.

* The KubeSphere cluster needs to have an Elasticsearch log receiver added. For more information, refer to the "Add Log Receiver" chapter in the *KubeSphere Cluster Management Guide*.

endif::[]